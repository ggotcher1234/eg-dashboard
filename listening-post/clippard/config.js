// Per-client config for Clippard Instrument Labs — the ONLY file that should
// differ between FeedMe deployments. Do not edit index.html for branding.
window.FMP_CONFIG = {
  clientId: "clippard",
  clientName: "Clippard Instrument Labs",
  // Skips the first-run "what's your company name" setup modal — goes
  // straight into the app already branded for Clippard.
  _setupComplete: true
};
